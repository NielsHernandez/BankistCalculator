//to make a varialbe nullable we have to use the ? nullable operataor after the data type


int? age = null;

age = 20;

Console.WriteLine(age);

string? name = Console.ReadLine();

if (name == null) Console.WriteLine("you did it type anything");

Console.WriteLine(name);
